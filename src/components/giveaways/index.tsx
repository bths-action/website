"use client";
import { FC } from "react";

export const Giveaways: FC = () => {
  return (
    <main>
      {/* <GiveawayForm mode="create" setOpen={() => {}} />                           */}
    </main>
  );
};
