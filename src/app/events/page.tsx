import { Events } from "@/components/events/events";
import { FC } from "react";

const EventsPage: FC = () => {
  return (
    <main>
      <h1>Events</h1>

      <Events />
    </main>
  );
};

export default EventsPage;
